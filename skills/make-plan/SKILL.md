---
name: make-plan
description: "把规格拆成有序、可验证的执行基线，并把测试和验收绑进完成定义。"
disable-model-invocation: true
---

# 拆计划 make-plan

计划的核心职能是**把测试和验收绑进「完成」的定义**：做完一步不等于代码写完，而是代码写完加验证通过。

计划落库，让执行可以跨 session 追溯，也让 `build-it` 有对照基线判断是否偏移。用户明确要口头计划、任务已缩成单步、或当前环境不能写文件时例外。

## 每个任务写四件事

```
改哪里    文件或模块范围
完成什么  这一步结束时什么可用
怎么验证  具体命令或操作，不是「测试通过」这种空话
预期看到  命令的预期输出或可观察结果
```

**没有验证方式的任务不算拆完。** 如果一步无法验证，它要么该合进相邻步骤，要么缺了可观察点——回到 `write-spec` 补。

## 排序

按依赖排，不按文件排。能独立交付的边界要标出来，让中途停下也是一个完整状态。

标出需要人工确认的位置：外部系统、真设备、付费资源、生产环境——**这些不因为写进计划就获得授权**。

## 硬线

- 关键步骤不留 `TBD`、`TODO`、「按需」、「类似」、「等等」
- 只写下游必须遵守的边界、产物、依赖和证据，实现细节留给执行者
- 计划无法拆解时，原因通常在上游：回到 `write-spec` 或 `make-sketch`

## 按需资源

规格定义了关键路径时，读取 `../prove-it/references/critical-path-verification.md`，把必需证据层级展开成前置条件、命令、预期证据、清理动作和失败退出条件。

## 交接

执行者能直接照计划进入 `build-it`、不必重新设计整体方案时停止。
